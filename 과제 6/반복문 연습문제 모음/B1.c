#include <stdio.h>

int main(void)
{
  int i, m, n, sum = 0, multiple = 1, factorial = 1, vib = 1, sum2 = 0;
  
  scanf("%d %d", &n);
  
  for(i = 1; i <= m; i++)
  {
	multiple *= n;
  }	
  return 0;
}